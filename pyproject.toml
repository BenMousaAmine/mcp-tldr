[project]
name = "mcp-tldr"
version = "0.1.0"
description = "MCP server wrapper for tldr code analysis tool"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
mcp-tldr = "server:main"
